#include <stdio.h>
int main()
{
    int profit, loss;
    printf("Enter the profit amount:");
    scanf("%d", &profit);
    printf("Enter the loss amount:");
    scanf("%d", &loss);
    
    if (profit > 0 && loss == 0);
    printf("The company is in profit of %d\n", profit);
    

}